# API Contract

Golden Look exposes exactly six backend API paths:

```text
POST /api/cases
POST /api/cases/[id]/parse
POST /api/cases/[id]/edit
GET /api/cases/[id]
POST /api/cases/[id]/publish
GET /api/flyer/[shareId]
```

Do not change these paths without team lead approval.
